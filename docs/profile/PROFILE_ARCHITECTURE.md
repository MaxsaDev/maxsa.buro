# Архітектура профілю користувача

## Огляд

Система профілю користувача побудована на принципах:

- **Консистентний UX/UI дизайн**
- **Зонування контенту** (Tabs)
- **Переиспользуемі компоненти**
- **Серверні компоненти де можливо**
- **Клієнтські компоненти тільки там де потрібна інтеракція**

---

## Структура файлів

```
app/(protected)/profile/
  └── page.tsx                    # Головна сторінка профілю (Client Component)

components/profile/
  ├── profile-info.tsx            # Основна інформація з inline-edit імені
  ├── change-password-form.tsx    # Форма зміни пароля
  ├── two-factor-setup.tsx        # Налаштування 2FA
  └── shared/                     # Переиспользуемі компоненти
      ├── profile-alert.tsx       # Уніфіковані алерти
      ├── profile-section.tsx     # Обгортка для секцій
      ├── profile-info-row.tsx    # Рядок з даними
      ├── profile-badge.tsx       # Бейджі статусів
      └── index.ts                # Експорти

components/passkey/
  ├── passkey-setup.tsx           # Додавання Passkey
  └── passkey-list.tsx            # Список Passkeys

actions/profile/
  ├── get-profile-data.ts         # Server Action для отримання даних
  └── update-name.ts              # Server Action для оновлення імені
```

---

## Зонування сторінки

Сторінка профілю розділена на **3 зони** за допомогою Shadcn UI Tabs:

### Зона 1: "Огляд" (Overview)

**Призначення:** Dashboard з оглядом ключової інформації та статусів

**Блоки:**

- **Основна інформація** (`ProfileInfo`)
  - Псевдонім (з inline-edit якщо системний)
  - Email
  - Роль
  - Статус верифікації email
  - Примітка про обмеження

- **Статус безпеки**
  - 2FA статус (увімкнено/вимкнено)
  - Passkey статус (кількість активних)

**UX-концепція:** Швидкий огляд найважливішої інформації без переходів між вкладками.

---

### Зона 2: "Персональні дані"

**Призначення:** Управління особистою інформацією користувача

**Статус:** 🚧 В розробці

**Заплановані блоки:**

1. **Персональні дані**
   - Повне ім'я (Прізвище + Ім'я + По батькові)
   - Дата народження
   - Стать

2. **Контактні дані**
   - Телефон (мінімум один обов'язковий)
   - Додаткові контакти

**UX-концепція:** Окрема зона для детальної особистої інформації, яка рідко змінюється.

---

### Зона 3: "Безпека" (Security)

**Призначення:** Всі налаштування безпеки облікового запису

**Блоки:**

- **Зміна пароля** (`ChangePasswordForm`)
- **Двохфакторна аутентифікація** (`TwoFactorSetupComponent`)
- **Додати Passkey** (`PasskeySetup`)
- **Мої Passkeys** (`PasskeyList`)

**UX-концепція:** Критичні налаштування безпеки в одному місці.

---

## Дизайн-система профілю

### Shared Components

#### 1. ProfileAlert

**Файл:** `components/profile/shared/profile-alert.tsx`

**Варіанти:**

- `info` - інформаційні повідомлення
- `success` - успішні операції
- `warning` - попередження
- `error` - помилки
- `note` - примітки (сірий фон для важливих підказок)

**Використання:**

```tsx
<ProfileAlert variant="note">
  <p>
    <strong>Примітка:</strong> Email не можна змінити.
  </p>
</ProfileAlert>
```

**Правило:**

- Використовується для **загальних підказок** в підвалі карти
- НЕ використовується для дрібних підказок під елементами (там звичайний текст)

---

#### 2. ProfileSection

**Файл:** `components/profile/shared/profile-section.tsx`

Обгортка для секцій з консистентним стилем:

- Заголовок з іконкою
- Опис
- Контент

```tsx
<ProfileSection
  title="Основна інформація"
  description="Дані вашого облікового запису"
  icon={<User className="size-5" />}
>
  {/* Контент */}
</ProfileSection>
```

---

#### 3. ProfileInfoRow

**Файл:** `components/profile/shared/profile-info-row.tsx`

Для відображення пар "ключ-значення":

```tsx
<ProfileInfoRow label="Email" value={user.email} />
<ProfileInfoRow
  label="Роль"
  value={<ProfileBadge variant="info">Адміністратор</ProfileBadge>}
/>
```

---

#### 4. ProfileBadge

**Файл:** `components/profile/shared/profile-badge.tsx`

Бейджі для статусів:

- `default` - звичайний
- `success` - зелений
- `warning` - помаранчевий
- `error` - червоний
- `info` - синій

---

## Паттерни валідації форм

### Консистентне відображення помилок

**Правило:** ProfileAlert наверху форми + червоний border на полях

#### Клієнтська валідація (RHF)

```tsx
{
  Object.keys(errors).length > 0 && (
    <ProfileAlert variant="error">
      <ul className="list-inside list-disc space-y-1">
        {errors.field && <li>{errors.field.message}</li>}
      </ul>
    </ProfileAlert>
  );
}

<Input className={errors.field ? 'border-red-500' : ''} {...register('field')} />;
```

#### Серверні помилки

```tsx
{
  serverError && <ProfileAlert variant="error">{serverError}</ProfileAlert>;
}
```

#### Успішні операції

```tsx
{
  serverSuccess && <ProfileAlert variant="success">{serverSuccess}</ProfileAlert>;
}
```

---

## Паттерн валідаційних галочок

**Компонент:** `CheckCircle2` з умовним рендерингом

**Правила відображення:**

1. Поле має бути `touched` (користувач взаємодіяв)
2. Немає помилок валідації
3. Поле **не порожнє** (перевірка через `watch()`)

**Позиціонування:**

- **Звичайні поля:** `right-3` (крайня позиція)
- **Password поля:**
  - Іконка "око": `right-12`
  - Галочка: `right-3` (крайня позиція)

```tsx
const fieldValue = watch('field');

{
  touchedFields.field && !errors.field && fieldValue && fieldValue.length > 0 && (
    <CheckCircle2 className="absolute right-3 top-1/2 size-5 -translate-y-1/2 text-green-600" />
  );
}
```

**Результат:** Всі галочки вирівняні вертикально, не "стрибають".

---

## PasswordInput компонент

**Файл:** `components/ui/password-input.tsx`

**Особливості:**

- Padding: `pr-20` (місце для двох іконок)
- Іконка "око": `right-12`
- Зовнішня галочка: `right-3`

---

## Inline Editing паттерн

**Приклад:** Редагування псевдоніма в `ProfileInfo`

**UX Flow:**

1. Відображення з кнопкою "редагувати" (якщо можна редагувати)
2. Клік → Input з'являється на місці тексту
3. Кнопки підтвердження/скасування під курсором
4. AlertDialog для фінального підтвердження
5. Успіх → повернення до режиму перегляду

**Переваги:**

- Компактність
- Контекст не втрачається
- Зрозуміло що редагується

---

## Server Actions

### getProfileData()

**Файл:** `actions/profile/get-profile-data.ts`

Централізоване отримання даних профілю:

- 2FA статус
- Список Passkeys
- Користувацькі дані

**Використання:**

```tsx
const data = await getProfileData();
```

**Переваги:**

- Одна точка отримання даних
- Менше дублювання коду
- Легше підтримувати

---

## Концепції іконок

**Правило:** Кожна карта має іконку в заголовку

**Маппінг:**

- User - Основна інформація
- Lock - Зміна пароля
- Shield - 2FA / Безпека
- Key - Passkeys
- FileText - Персональні дані

---

## Мобільна адаптація

**Принципи:**

- Tabs: горизонтальна прокрутка на маленьких екранах
- Кнопки: `w-full` на мобільних, `flex-1` або `flex` на десктопі
- Двостовпцевий грід в "Статус безпеки": `grid-cols-1 sm:grid-cols-2`

---

## Майбутні поліпшення

### Пріоритет 1: Завершення зони "Персональні дані"

- [ ] Блок "Персональні дані" (ім'я, дата, стать)
- [ ] Блок "Контактні дані" (телефон мінімум)

### Пріоритет 2: Рефакторинг на RSC

- [ ] Перенести сторінку профілю на Server Component
- [ ] Залишити Client Components тільки для форм
- [ ] Оптимізувати завантаження даних

### Пріоритет 3: Розширення

- [ ] Завантаження аватара
- [ ] Історія змін
- [ ] Налаштування приватності

---

## Best Practices

### 1. Одна кнопка = повна ширина

Якщо в блоці одна кнопка - вона має займати всю ширину (`w-full` або `flex-1`).

### 2. Дві кнопки = рівномірний розподіл

```tsx
<div className="flex gap-2">
  <Button className="flex-1">Кнопка 1</Button>
  <Button className="flex-1">Кнопка 2</Button>
</div>
```

### 3. Підказки

- **Дрібні під елементами:** `<p className="text-muted-foreground text-xs">`
- **Загальні в підвалі карти:** `<ProfileAlert variant="note">`

### 4. Діалоги

- Уникати форм в діалогах (z-index проблеми)
- Розміщувати поля безпосередньо в карті
- AlertDialog тільки для підтверджень

### 5. Псевдонім vs Повне ім'я

- **Псевдонім** - латиниця, без пробілів, один раз можна змінити
- **Повне ім'я** - кирилиця, в блоці "Персональні дані"

---

## Термінологія

- **Псевдонім** (nickname) - поле `name` в БД, використовується для входу
- **Повне ім'я** - окремі поля в блоці персональних даних
- **Email** - незмінне поле, тільки адмін може змінити
- **Passkey** - WebAuthn ключ для входу без пароля
- **2FA** - TOTP двохфакторна аутентифікація

---

**Дата створення:** 2025-01-12
**Версія:** 1.0
**Автор:** AI Assistant + User Collaboration
