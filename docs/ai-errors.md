# AI Errors - Критичні помилки при розробці

## ⚠️ ВАЖЛИВО

Цей документ містить критичні помилки, які були допущені під час розробки функціоналу персональних даних. Повторення цих помилок **КАТЕГОРИЧНО ЗАБОРОНЕНО**.

---

## 1. ❌ SQL запити в папці `actions/`

### Що було зроблено неправильно:

У файлі `actions/profile/update-name.ts` були написані прямі SQL запити:

```typescript
// ❌ НЕПРАВИЛЬНО - SQL в actions
export const updateNameAction = async () => {
  await pool.query('UPDATE "user" SET name = $1 WHERE id = $2', [newName, userId]);
};
```

### Чому це помилка:

Згідно з правилами проекту, структура має бути:

- `/data/[schema-name]/[table-name].ts` - **ТІЛЬКИ SQL запити**
- `/actions/` - валідація, виклик data функцій, повернення ActionStatus

### Правильне рішення:

```typescript
// ✅ ПРАВИЛЬНО
// data/auth/user.ts
export async function updateUserName(userId: string, newName: string) {
  // SQL тут
}

// actions/profile/update-name.ts
export async function updateNameAction() {
  // валідація
  await updateUserName(user.id, newName); // виклик data функції
  return ActionStatus;
}
```

### Наслідки:

Довелося повністю рефакторити весь код, створювати `data/auth/user.ts`, переписувати всі actions.

---

## 2. ❌ Порушення нейминга файлів в `/data`

### Що було зроблено неправильно:

Створено файл `/data/mx-data/personal-data.ts`, хоча в БД **немає** таблиці `personal_data`.

### Чому це помилка:

Правило нейминга чітко встановлено:

```
/data/[schema-name]/[table-name].ts
```

Якщо таблиця називається `user_data`, файл має бути `user-data.ts`.
Якщо таблиця називається `user_contact`, файл має бути `user-contact.ts`.

### Правильне рішення:

Функція `savePersonalDataWithContacts()` (яка працює з `user_data` + `user_contact`) має бути в файлі `user-data.ts`, бо вона створює запис в `user_data`.

### Наслідки:

Довелося видаляти файл `personal-data.ts`, переносити функції в `user-data.ts`, оновлювати всі імпорти.

---

## 3. ❌ Відсутність транзакцій в data функціях

### Що було зроблено неправильно:

Data функції робили прямі запити без транзакцій:

```typescript
// ❌ НЕПРАВИЛЬНО
export async function setDefaultContact(userId, contactId) {
  const result = await pool.query('UPDATE ...');
  return result;
}
```

### Чому це помилка:

Згідно з правилами проекту, **всі** INSERT/UPDATE/DELETE **обов'язково** повинні бути обгорнуті в транзакції:

```typescript
const client = await pool.connect();
try {
  await client.query('BEGIN');
  // ... SQL
  await client.query('COMMIT');
} catch (error) {
  await client.query('ROLLBACK');
  throw error;
} finally {
  client.release();
}
```

### Правильне рішення:

Кожна функція з модифікацією даних має використовувати транзакції.

### Наслідки:

Довелося переписувати **всі** data функції у всіх файлах (`user.ts`, `user-data.ts`, `user-contact.ts`, `users.ts`).

---

## 4. ❌ Дублювання `updated_at = NOW()` в коді

### Що було зроблено неправильно:

Вручну встановлювали `updated_at` в SQL запитах:

```typescript
// ❌ НЕПРАВИЛЬНО
UPDATE user_contact SET is_default = true, updated_at = NOW()
```

І в триггерах БД:

```sql
-- ❌ НЕПРАВИЛЬНО
UPDATE user_contact
SET is_default = FALSE, updated_at = NOW()
```

### Чому це помилка:

В БД є **BEFORE UPDATE триггер** `trg_user_contact_bu_set_updated_at`, який **автоматично** встановлює `updated_at = NOW()` для **кожного** UPDATE.

Писати `updated_at = NOW()` в коді = **дублювання логіки**.

### Правильне рішення:

```typescript
// ✅ ПРАВИЛЬНО - updated_at встановиться автоматично
UPDATE user_contact SET is_default = true
```

### Наслідки:

Довелося видаляти `updated_at = NOW()` з:

- Всіх data функцій
- Всіх SQL триггерів
- Додавати коментарі про автоматичне встановлення

---

## 5. ❌ Використання `router.refresh()` для Client Component з локальним state

### Що було зроблено неправильно:

Компонент `PersonalDataSection` - це Client Component з локальним state (`useState`).
Після операцій викликався `router.refresh()`:

```typescript
// ❌ НЕПРАВИЛЬНО
const handleSetDefault = async (contactId) => {
  await setDefaultContactAction(contactId);
  router.refresh(); // ← не оновлює локальний state!
};
```

### Чому це помилка:

`router.refresh()` оновлює **Server Components**, але не оновлює **локальний state** Client Component.

Результат: дані в БД змінились, але UI не оновлюється → потрібна перезагрузка сторінки.

### Правильне рішення:

Передавати callback-функцію `loadData()`:

```typescript
// ✅ ПРАВИЛЬНО
const handleSetDefault = async (contactId) => {
  await setDefaultContactAction(contactId);
  await loadData(); // ← перезавантажує дані, оновлює state
};

// Parent передає callback
<AddContactForm onContactAdded={loadData} />

// Child викликає після успіху
if (result.status === 'success') {
  await onContactAdded();
}
```

### Наслідки:

Довелося додавати callback props в усі компоненти: `EditFullNameForm`, `AddContactForm`, `PersonalDataSection`.

---

## 6. ❌ Неправильна робота з BEFORE UPDATE триггером та unique index

### Що було зроблено неправильно:

Спочатку логіка зміни дефолтного контакту була така:

```typescript
// ❌ НЕПРАВИЛЬНО
UPDATE user_contact SET is_default = true WHERE id = X
```

При цьому існував **AFTER UPDATE триггер**, який мав знімати `is_default` з інших контактів.

### Чому це помилка:

Порядок виконання PostgreSQL:

1. UPDATE запит
2. Перевірка unique index `user_contact_default_one_per_user_idx`
3. **ТІЛЬКИ ПОТІМ** триггер

Результат: **race condition** → помилка `duplicate key value violates unique constraint`.

### Правильне рішення:

Використовувати **BEFORE UPDATE триггер**:

```sql
-- ✅ ПРАВИЛЬНО
CREATE TRIGGER trg_user_contact_bu_maintain_default
BEFORE UPDATE ON mx_data.user_contact
```

Триггер спрацює **ДО** перевірки індексу і зніме `is_default` з інших контактів.

В коді залишається просто:

```typescript
// ✅ ПРАВИЛЬНО - триггер сам знімає з інших
UPDATE user_contact SET is_default = true WHERE id = X
```

### Наслідки:

Довелося:

1. Розділити триггери на BEFORE UPDATE та AFTER INSERT/DELETE
2. Спростити data функцію `setDefaultContact()`
3. Оновити коментарі в коді

---

## 7. ❌ Ігнорування компонентів з shadcn UI

### Що було зроблено неправильно:

Створювались самописні div-панелі для відображення контактів:

```tsx
// ❌ НЕПРАВИЛЬНО
<div className="flex items-center justify-between rounded-md border p-3">
  <div className="flex-1">
    <p className="text-sm font-medium">{title}</p>
    <p className="text-muted-foreground text-sm">{value}</p>
  </div>
  <div className="flex items-center gap-2">
    <Button>...</Button>
  </div>
</div>
```

### Чому це помилка:

В проекті використовується **shadcn UI**, яка має готовий компонент **Item** для таких випадків:
https://ui.shadcn.com/docs/components/item

### Правильне рішення:

```tsx
// ✅ ПРАВИЛЬНО
<Item variant="outline">
  <ItemContent>
    <ItemTitle>{title}</ItemTitle>
    <ItemDescription>{value}</ItemDescription>
  </ItemContent>
  <ItemActions>
    <Button>...</Button>
  </ItemActions>
</Item>
```

### Наслідки:

Довелося встановлювати компонент Item та переписувати весь UI.

---

## 8. ❌ Використання стандартних `alert()` та `confirm()` замість shadcn UI компонентів

### Що було зроблено неправильно:

У компоненті `PersonalDataSection` використовувався стандартний браузерний `confirm()`:

```typescript
// ❌ НЕПРАВИЛЬНО
const handleDelete = async (contactId: string) => {
  if (!confirm('Ви впевнені, що хочете видалити цей контакт?')) {
    return;
  }
  // ... видалення
};
```

### Чому це помилка:

В проекті використовується **shadcn UI**, яка має готові компоненти для діалогів:

- `AlertDialog` — для підтвердження дій (замість `confirm()`)
- `toast` з бібліотеки `sonner` — для сповіщень (замість `alert()`)

Стандартні браузерні діалоги:

- ❌ Не стилізуються під дизайн додатку
- ❌ Блокують весь UI
- ❌ Виглядають застаріло
- ❌ Не підтримують доступність (a11y)
- ❌ Порушують UX консистентність

### Правильне рішення:

```typescript
// ✅ ПРАВИЛЬНО
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog';

// State для контролю діалогу
const [deleteContactId, setDeleteContactId] = useState<string | null>(null);

// Кнопка відкриває діалог
<AlertDialog open={!!deleteContactId} onOpenChange={(open) => !open && setDeleteContactId(null)}>
  <AlertDialogTrigger asChild>
    <Button onClick={() => setDeleteContactId(contact.id)}>
      <Trash2 />
    </Button>
  </AlertDialogTrigger>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Видалити контакт?</AlertDialogTitle>
      <AlertDialogDescription>
        Ця дія незворотня. Контакт буде видалено назавжди.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Скасувати</AlertDialogCancel>
      <AlertDialogAction onClick={() => handleDelete(deleteContactId!)}>
        Видалити
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

### Коли використовувати:

- **AlertDialog** — підтвердження деструктивних дій (видалення, скидання)
- **toast** — сповіщення про результат операцій (успіх/помилка)
- **Dialog** — складні форми та модальні вікна

### Наслідки:

Довелося переписувати компонент `PersonalDataSection`, додавати state для контролю діалогу та замінювати всі виклики `confirm()`.

---

## 9. ❌ Зелена галочка валідації з'являється при неповному заповненні поля

### Що було зроблено неправильно:

У компоненті `ChangePasswordForm` зелена галочка (поощрительна іконка) з'являлася після введення тільки 1 символу:

```typescript
// ❌ НЕПРАВИЛЬНО
{touchedFields.currentPassword &&
  !errors.currentPassword &&
  currentPasswordValue &&
  currentPasswordValue.length > 0 && ( // ← тільки 1 символ!
    <CheckCircle2 className="text-green-600" />
  )}
```

Але мінімальні вимоги до пароля - **8 символів**, а не 1.

### Чому це помилка:

**Зелена галочка** - це **поощрительна іконка** для користувача, яка повідомляє: "Ви все зробили правильно, це значення валідне".

Якщо галочка з'являється при 1 символі, коли мінімум - 8, це:

- ❌ Вводить користувача в оману
- ❌ Порушує логіку валідації
- ❌ Погіршує UX (користувач думає що все ОК, але отримує помилку при submit)
- ❌ Суперечить схемі валідації Zod

### Правильне рішення:

**ЗОЛОТЕ ПРАВИЛО:** Зелена галочка (поощрительна іконка) з'являється **ТІЛЬКИ** коли значення в полі:

1. ✅ Відповідає **ВСІМ** мінімальним вимогам схеми Zod
2. ✅ Не має помилок валідації
3. ✅ Поле було touched (користувач взаємодіяв з ним)

```typescript
// ✅ ПРАВИЛЬНО для пароля (мінімум 8 символів)
{touchedFields.currentPassword &&
  !errors.currentPassword &&
  currentPasswordValue &&
  currentPasswordValue.length >= 8 && ( // ← мінімум 8 символів
    <CheckCircle2 className="text-green-600" />
  )}

// ✅ ПРАВИЛЬНО для підтвердження пароля (8 символів + співпадіння)
{touchedFields.confirmPassword &&
  !errors.confirmPassword &&
  confirmPasswordValue &&
  confirmPasswordValue.length >= 8 &&
  confirmPasswordValue === newPasswordValue && ( // ← додаткова умова
    <CheckCircle2 className="text-green-600" />
  )}

// ✅ ПРАВИЛЬНО для повного імені (мінімум 2 символи + regex)
{isFieldValid && fullNameValue && fullNameValue.trim().length >= 2 && (
  <CheckCircle2 className="text-green-600" />
)}
```

### Схема валідації також має бути правильною:

```typescript
// ❌ НЕПРАВИЛЬНО - поточний пароль не може бути 1 символ
const changePasswordSchema = z.object({
  currentPassword: z.string().min(1, { message: 'Введіть поточний пароль' }),
  // ...
});

// ✅ ПРАВИЛЬНО - всі паролі мінімум 8 символів
const changePasswordSchema = z.object({
  currentPassword: z
    .string()
    .min(8, { message: 'Поточний пароль має містити щонайменше 8 символів' }),
  newPassword: z.string().min(8, { message: 'Новий пароль має містити щонайменше 8 символів' }),
  confirmPassword: z
    .string()
    .min(8, { message: 'Підтвердження паролю має містити щонайменше 8 символів' }),
});
```

### Наслідки:

Довелося виправляти:

1. Схему валідації Zod (мінімум з 1 на 8 символів)
2. Умови показу зеленої галочки в усіх трьох полях
3. Placeholder-и в полях (додати підказку про мінімальну довжину)

---

## 📚 Висновки для майбутнього

### ✅ Завжди робити:

1. **Читати правила проекту ПЕРЕД написанням коду**
   - `.cursor/rules`
   - `docs/`
   - Технічне завдання

2. **Дотримуватись архітектури:**
   - SQL тільки в `/data`
   - Транзакції для всіх змін
   - Нейминг по імені таблиці

3. **Не дублювати логіку:**
   - Якщо є триггер `updated_at` → не писати вручну
   - Якщо є BEFORE триггер → використовувати його

4. **Використовувати готові компоненти:**
   - shadcn UI компоненти
   - Не винаходити велосипед

5. **Правильно оновлювати UI:**
   - Client Component з state → callback
   - Server Component → `router.refresh()`

### ❌ Ніколи не робити:

1. ❌ SQL в `actions/`
2. ❌ Порушувати нейминг файлів
3. ❌ Змінювати дані без транзакцій
4. ❌ Дублювати логіку, яка є в БД
5. ❌ Використовувати `router.refresh()` для локального state
6. ❌ Писати самописні компоненти замість shadcn UI
7. ❌ Використовувати стандартні `alert()` / `confirm()` замість shadcn UI діалогів
8. ❌ Показувати зелену галочку валідації до досягнення мінімальних вимог схеми

---

## 🎯 Золоте правило

**ПЕРЕД початком розробки:**

1. Прочитати технічне завдання **ПОВНІСТЮ**
2. Звернути увагу на **всі** зауваження користувача
3. Дотримуватись **існуючих** правил проекту
4. Не робити припущень - якщо щось незрозуміло → **запитати**

**Якщо користувач вказав на помилку:**

- Це означає, що **я пропустив важливу інформацію**
- Треба виправити **НЕГАЙНО**
- Запам'ятати, щоб **не повторювати**
