# Перемикач теми (світла/темна)

## 📌 Мета

Додати перемикання між світлою та темною темами для всього застосунку, з доступним і очікуваним розміщенням у `Navbar` на захищених сторінках.

---

## ✅ Що реалізовано

- Провайдер тем на рівні `RootLayout`
- Кнопка перемикання теми в `Navbar`
- Збереження теми через `next-themes`
- Підтримка системної теми (`system`)
- Плавне перемикання без миготіння

---

## 🧭 Золотий шлях (рекомендований підхід)

Цей шлях гарантує стабільну зміну теми та консистентні кольори в усіх компонентах.

### 1) Базова тема в `globals.css`

Тут живе основний набір токенів (`:root` і `.dark`) та налаштування Tailwind через `@theme inline`.

### 2) Окремий файл для доповнень теми

Створи файл `app/theme-overrides.css` і зберігай у ньому **всі додаткові токени та кастомні правила**, які не генерує редактор тем.

Це дозволяє безпечно оновлювати тему через генератор (наприклад tweakcn) без втрати твоїх доповнень.

### 3) Підключення після `globals.css`

У `app/layout.tsx` імпортуй:

1. `./globals.css`
2. `./theme-overrides.css`

Це гарантує, що всі кастомні токени мають пріоритет над базовою темою.

### 4) Використання семантичних токенів

В компонентах використовуй тільки семантичні класи:

- `bg-success/10`, `text-success`
- `bg-warning/10`, `text-warning`
- `bg-info/10`, `text-info`
- `bg-destructive/10`, `text-destructive`

Це забезпечує автоматичну коректність кольорів у світлій/темній темі.

### 5) Для складних станів — власні CSS‑токени

Для UI, де потрібні тонкі відмінності (наприклад, різна інтенсивність активності), створюй окремі CSS‑токени в `theme-overrides.css` і прив’язуй класи в компонентах.

Приклад для меню:

- `--menu-category-weak`
- `--menu-category-strong`
- `--menu-item-active`

---

## ⚠️ Попередження (що НЕ працює)

- **Не змішуй жорсткі кольори** типу `bg-green-50`, `text-blue-600` у компонентах — це ламає темну тему.
- **Не зберігай кастомні токени в `globals.css`**, якщо ти оновлюєш тему через генератор — вони будуть перезаписані.
- **Не використовуй `dark:` як основний метод темізації** — це не масштабований підхід і швидко призводить до розсинхрону.

---

## 🧩 Архітектура

### 1) Провайдер тем

Файл: `components/theme-provider.tsx`

Провайдер обгортає весь застосунок і керує темою через клас `dark` на елементі `html`.

### 2) Перемикач теми

Файл: `components/theme-toggle.tsx`

Кнопка у вигляді іконки, перемикає між `light` та `dark`.  
Використано `useTheme()` з `next-themes`.

### 3) Підключення у layout

Файл: `app/layout.tsx`

Провайдер розміщений на рівні `RootLayout`, щоб тема працювала глобально.

### 4) Розділення теми і доповнень

Файл: `app/theme-overrides.css`

Містить усі токени для кастомних станів інтерфейсу та специфічних сценаріїв.  
Цей файл повинен зберігатись завжди — незалежно від заміни базової теми.

---

## 📍 Розміщення в UI

Перемикач додано у `Navbar` захищеного лейауту:

Файл: `app/(protected)/layout.tsx`

Це забезпечує:

- швидкий доступ із будь-якої захищеної сторінки
- логічне розміщення поруч з навігацією
- мінімальне захаращення меню

---

## ♿ Доступність

Кнопка має:

- `aria-label="Змінити тему"`
- `title="Змінити тему"`
- прихований текст для screen readers

---

## ⚙️ Налаштування та поведінка

Провайдер має параметри:

- `attribute="class"` — використання класу `dark` для теми
- `defaultTheme="system"` — повага до системної теми
- `enableSystem` — увімкнення системної теми
- `disableTransitionOnChange` — прибирає миготіння при зміні теми

---

## 🎨 Адаптація кольорів інтерфейсу

### Основний принцип

Усі кольори мають проходити через токени (CSS‑змінні).  
У компонентах використовуються тільки семантичні класи.

### Де зберігаються токени

- Базові токени теми: `app/globals.css`
- Додаткові токени: `app/theme-overrides.css`

### Приклад для станів меню

У `theme-overrides.css` визначаються токени:

- `--menu-category-weak`
- `--menu-category-strong`
- `--menu-item-active`
- `--menu-media-primary`, `--menu-media-success`

У компонентах використовуються відповідні CSS‑класи:

- `.menu-active-category`
- `.menu-active-item`
- `.menu-active-media-primary`
- `.menu-active-media-success`

---

## 📦 Залежності

Використано:

- `next-themes`
- `lucide-react` (іконки)

---

## 🧪 Швидка перевірка

1. Відкрий будь-яку захищену сторінку
2. Натисни іконку теми в `Navbar`
3. Перевір зміну кольорової схеми
4. Перезавантаж сторінку — тема має зберігатися

---

## 🔗 Пов’язані файли

- `components/theme-provider.tsx`
- `components/theme-toggle.tsx`
- `app/layout.tsx`
- `app/(protected)/layout.tsx`
- `app/globals.css`
- `app/theme-overrides.css`
- `components/mx-admin/user-data/user-menu.tsx`
- `components/mx-admin/user-data/user-permissions.tsx`
