# Сумісність з React Compiler (Next.js 16)

## Проблема

TanStack Table v8 використовує `useReactTable()` хук, який повертає об'єкт з функціями, що змінюються при кожному рендері. React Compiler не може безпечно мемоізувати такі функції, що призводить до попередження:

```
⚠️ Compilation Skipped: Use of incompatible library
TanStack Table's useReactTable() API returns functions that
cannot be memoized safely
```

## Технічні деталі

### Чому виникає попередження?

1. **Динамічні функції**: `useReactTable()` повертає об'єкт з методами, які створюються заново при кожному рендері
2. **Внутрішній стан**: Таблиця тримає складний внутрішній стан, який постійно оновлюється
3. **Референційна нестабільність**: Функції в об'єкті `table` не зберігають референцію між рендерами

### Чи впливає це на продуктивність?

**НІ!** Це попередження не створює проблем з продуктивністю:

- TanStack Table вже оптимізований внутрішньо
- Компонент таблиці рендериться тільки при зміні даних
- Next.js 16 SSR мінімізує кількість клієнтського JavaScript
- React Virtual DOM ефективно обробляє оновлення

## Реалізоване рішення

### 1. Документування в коді

Додано детальний коментар на початку файлу:

```typescript
/**
 * ⚠️ React Compiler Notice:
 * TanStack Table v8 використовує useReactTable(), який повертає функції що не можуть
 * бути безпечно мемоізовані. React Compiler автоматично пропускає мемоізацію цього
 * компонента. Це очікувана поведінка і не впливає на функціональність.
 */
```

### 2. ESLint комментар

Додано ESLint директиву для підавлення попередження:

```typescript
// eslint-disable-next-line react-hooks/incompatible-library -- TanStack Table v8 поки не повністю сумісний з React Compiler
const table = useReactTable({
  // ...
});
```

### 3. Оновлено ESLint конфігурацію

В `eslint.config.mjs` додано ігнорування папки `temp/`:

```javascript
globalIgnores([
  '.next/**',
  'out/**',
  'build/**',
  'next-env.d.ts',
  'node_modules/**',
  'temp/**', // Тимчасові файли та архіви
]);
```

### 4. Архітектурні рішення

Мінімізовано вплив обмеження:

- ✅ **Server Components**: Дані завантажуються на сервері
- ✅ **Client Boundary**: Клієнтський компонент тільки для інтерактивності
- ✅ **Оптимізований стейт**: Мінімум локального стану в компоненті
- ✅ **Правильна структура**: Розділення логіки та UI

## Альтернативні підходи

### 1. Використання `'use no memo'` директиви

```typescript
'use no memo';

export function DataTable({ columns, data }: Props) {
  // ...
}
```

**Висновок**: Не потрібно, оскільки React Compiler вже автоматично пропускає мемоізацію.

### 2. Ручна мемоізація з `useMemo`

```typescript
const table = useMemo(
  () =>
    createReactTable({
      // ...
    }),
  [data, columns]
);
```

**Висновок**: Не рекомендується для Next.js 16, оскільки React Compiler автоматично оптимізує.

### 3. Перехід на іншу бібліотеку таблиць

**Висновок**: Недоцільно, TanStack Table - найкраще рішення для складних таблиць.

## Результати

### До змін

```bash
✖ 3 problems (0 errors, 3 warnings)
```

### Після змін

```bash
✓ ESLint: 0 errors, 0 warnings
✓ TypeScript: No errors
✓ Build: Success
```

## Майбутні оновлення

### TanStack Table v9 (очікується)

Розробники TanStack Table працюють над повною сумісністю з React Compiler:

- Стабільні референції функцій
- Оптимізована структура стейту
- Нативна підтримка React Compiler

**Посилання**:

- [TanStack Table Discussion #5280](https://github.com/TanStack/table/discussions/5280)
- [React Compiler Documentation](https://react.dev/learn/react-compiler)

### Що потрібно зробити при оновленні?

1. Оновити `@tanstack/react-table` до v9+
2. Видалити ESLint коментар
3. Видалити документуючі коментарі про обмеження
4. Перевірити, що React Compiler успішно мемоізує компонент

## Висновки

### ✅ Зроблено правильно

- Задокументовано обмеження в коді
- Додано пояснювальні коментарі
- Оновлено ESLint конфігурацію
- Мінімізовано вплив архітектурними рішеннями
- Оновлено документацію проекту

### 📚 Уроки

1. **Не всі бібліотеки готові до React Compiler** - це нормально
2. **Попередження != Помилка** - важливо розуміти різницю
3. **Документація в коді** - допомагає майбутнім розробникам
4. **Правильна архітектура** - мінімізує вплив обмежень бібліотек

### 🎯 Для інших проектів

Якщо зустрічаєте подібне попередження:

1. Перевірте, чи це відома проблема бібліотеки
2. Задокументуйте причину в коді
3. Додайте ESLint комментар з поясненням
4. Оновіть документацію проекту
5. Слідкуйте за оновленнями бібліотеки

---

**Автор**: Cursor AI + maxsa
**Дата**: 12 листопада 2025
**Версія**: 1.0
