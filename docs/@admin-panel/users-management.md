# Управління користувачами (Admin Panel)

## Опис

Сторінка адміністратора для управління користувачами системи. Забезпечує повний контроль над обліковими записами користувачів, включаючи зміну ролей, керування безпекою та модерацію.

## Розташування файлів

### Сторінка

- **Page**: `/app/(protected)/mx-admin/users/page.tsx`

### Табличні компоненти

- **Wrapper**: `/components/tables/public/user-view/table-wrapper.tsx`
- **Table**: `/components/tables/public/user-view/data-table.tsx`
- **Columns**: `/components/tables/public/user-view/columns.tsx`

### Управляючі компоненти

- **Toggle Role**: `/components/mx-admin/users/toggle-role-button.tsx`
- **Disable 2FA**: `/components/mx-admin/users/disable-two-factor-button.tsx`
- **Disable Passkey**: `/components/mx-admin/users/disable-passkey-button.tsx`
- **Resend Verification**: `/components/mx-admin/users/resend-verification-button.tsx`
- **Toggle Ban**: `/components/mx-admin/users/toggle-ban-button.tsx`
- **Delete User**: `/components/mx-admin/users/delete-user-button.tsx`

### Екшени (Server Actions)

- **Toggle Role**: `/actions/auth/toggle-user-role.ts`
- **Disable 2FA**: `/actions/auth/disable-two-factor.ts`
- **Disable Passkey**: `/actions/auth/disable-passkey.ts`
- **Resend Verification**: `/actions/auth/resend-verification.ts`
- **Toggle Ban**: `/actions/auth/toggle-ban.ts`
- **Delete User**: `/actions/auth/delete-user.ts`

### Інтерфейси та дані

- **Interface**: `/interfaces/public/user-view.ts`
- **Data Layer**: `/data/auth/user-view.ts`

## Структура таблиці

### Відображувані колонки

1. **Статус** (`iconStatus`)
   - Crown (👑) - адміністратор
   - Ban (🚫) - заблокований користувач
   - Dash (—) - звичайний користувач

2. **Аватар** (`image`)
   - Зображення користувача або ініціали

3. **Ім'я** (`name`)
   - Основне: зареєстроване ім'я
   - Додаткове: повне ім'я (`full_name`)

4. **Email** (`email`)
   - Основне: email адреса
   - Додаткове: статус верифікації (зелений/червоний)

5. **Дії** (`actions`)
   - Набір управляючих кнопок

### Приховані колонки (для пошуку)

- `id`, `full_name`, `email_verified`, `role`, `is_banned`,
- `two_factor_enabled`, `has_passkey`, `created_at`, `updated_at`

## Функціональність

### 1. Глобальний пошук

- Пошук по полях: `name`, `full_name`, `email`
- Використовується **Fuzzy Filter** з `@tanstack/match-sorter-utils`
- Миттєве оновлення результатів
- Підрахунок знайдених записів

### 2. Пагінація

- 10 записів на сторінку (за замовчуванням)
- Навігація по сторінках
- Відображення поточної сторінки та загальної кількості

### 3. Управляючі дії

#### A. Зміна ролі (user ↔ admin)

- **Іконка**: Crown (адмін) / User (користувач)
- **Доступність**: Недоступна для забанених
- **Обмеження**: Не можна змінити власну роль
- **Підтвердження**: AlertDialog

#### B. Відключення 2FA

- **Іконка**: ShieldOff
- **Доступність**: Активна лише якщо 2FA увімкнено
- **Доступність**: Недоступна для забанених
- **Результат**: Видаляє секрет та скидає прапорець `two_factor_enabled`
- **Підтвердження**: AlertDialog

#### C. Відключення Passkey

- **Іконка**: KeyRound
- **Доступність**: Активна лише якщо є Passkey
- **Доступність**: Недоступна для забанених
- **Результат**: Видаляє всі Passkey користувача
- **Підтвердження**: AlertDialog

#### D. Повторна відправка верифікаційного листа

- **Іконка**: MailCheck
- **Доступність**: Активна лише якщо email не підтверджено
- **Доступність**: Недоступна для забанених
- **Результат**: Відправляє новий верифікаційний лист
- **Підтвердження**: AlertDialog

#### E. Бан/Розбан

- **Іконка**: Ban (бан) / CheckCircle (розбан)
- **Доступність**: Завжди активна
- **Обмеження**: Не можна забанити самого себе
- **Результат**: Змінює статус `banned`
- **Підтвердження**: AlertDialog

#### F. Видалення користувача

- **Іконка**: Trash2 (червона)
- **Доступність**: Завжди активна (єдина для забанених)
- **Обмеження**: Не можна видалити самого себе
- **Результат**: Повне видалення користувача (каскадне)
- **Підтвердження**: Спеціальний діалог з вводом слова "DELETE"

## Особливості реалізації

### Заблоковані користувачі

Для забанених користувачів (`is_banned: true`):

- ❌ Всі управляючі кнопки неактивні
- ✅ Тільки кнопка видалення активна

### Захист адміністратора

- Адміністратор не може змінити власну роль
- Адміністратор не може забанити самого себе
- Адміністратор не може видалити самого себе

### Підтвердження дій

- Всі дії вимагають підтвердження через `AlertDialog`
- Видалення вимагає введення ключового слова "DELETE"
- Успішні дії показують toast-повідомлення

### Валідація

- Перевірка прав адміністратора на сервері
- Перевірка існування користувача
- Перевірка поточного статусу перед виконанням дії

## Технічний стек

### Основні бібліотеки

- **TanStack Table v8**: Основа для таблиці
- **@tanstack/match-sorter-utils**: Fuzzy search
- **Shadcn UI**: UI компоненти
- **Sonner**: Toast-повідомлення
- **Lucide React**: Іконки

### React Server Components

- Сторінка рендериться на сервері
- Дані завантажуються один раз при завантаженні сторінки
- Клієнтські компоненти лише для інтерактивності

### Оптимізація

- Мінімізація клієнтського JavaScript
- Server Actions для всіх мутацій
- `revalidatePath` для оновлення даних після змін

## Права доступу

### Доступ до сторінки

```typescript
// Перевірка ролі на сторінці
if (user.role !== 'admin') {
  redirect('/dashboard');
}
```

### Доступ до екшенів

```typescript
// Перевірка в кожному екшені
const currentUser = await getCurrentUser();
if (!currentUser || currentUser.role !== 'admin') {
  return { success: false, error: 'Недостатньо прав' };
}
```

## База даних

### View: user_view

```sql
SELECT
  id,
  name,
  full_name,
  email,
  email_verified,
  image,
  role,
  is_banned,
  two_factor_enabled,
  has_passkey,
  created_at,
  updated_at
FROM user_view
ORDER BY created_at DESC;
```

### Каскадне видалення

При видаленні користувача автоматично видаляються:

- Всі сесії
- Всі Passkey
- Всі токени верифікації
- Інші пов'язані дані (згідно з налаштуваннями БД)

## Приклади використання

### Зміна ролі користувача

```typescript
const result = await toggleUserRole(userId);
if (result.success) {
  toast.success('Роль змінено');
}
```

### Видалення користувача

```typescript
const result = await deleteUser(userId, 'DELETE');
if (result.success) {
  toast.success('Користувача видалено');
}
```

## Можливі розширення

- [ ] Масові операції (вибір декількох користувачів)
- [ ] Експорт даних користувачів (CSV/Excel)
- [ ] Історія дій адміністратора (audit log)
- [ ] Фільтри (за роллю, статусом бану, 2FA)
- [ ] Сортування по різним полям
- [ ] Розширений пошук з фільтрами
- [ ] Статистика по користувачам

## Відомі обмеження

### React Compiler Warning

```
⚠️ Compilation Skipped: Use of incompatible library
TanStack Table's useReactTable() API returns functions that
cannot be memoized safely
```

**Пояснення**: Це відоме обмеження TanStack Table v8 з React Compiler. Не впливає на функціональність, але компонент не буде автоматично мемоізований React Compiler.

**Технічні деталі**:

- `useReactTable()` повертає об'єкт з функціями, які змінюються при кожному рендері
- React Compiler не може безпечно мемоізувати такі функції
- Це очікувана поведінка і не створює проблем з продуктивністю

**Реалізоване рішення**:

- ✅ Додано детальні коментарі в код про причину попередження
- ✅ Додано ESLint комментар для підавлення попередження
- ✅ Компонент оптимізований вручну де необхідно
- ✅ Використано Next.js 16 SSR для мінімізації клієнтського JavaScript
- ✅ Папка `temp/` додана в `.eslintignore`

**Посилання**: [TanStack Table Discussion #5280](https://github.com/TanStack/table/discussions/5280)

**Очікується**: Оновлення TanStack Table для повної сумісності з React Compiler в майбутніх версіях.

## Безпека

### SQL Injection

- Всі запити використовують параметризовані запити
- `pool.query` з плейсхолдерами `$1`, `$2` тощо

### XSS

- Всі дані користувача екрануються React
- HTML entities у AlertDialog описах

### CSRF

- Better Auth автоматично захищає від CSRF
- Server Actions захищені Next.js

### Authorization

- Перевірка ролі на кожному рівні
- Сервер завжди перевіряє права доступу

## Логування

### Важливі події

- Зміна ролі користувача
- Бан/розбан користувача
- Видалення користувача

### Формат

```typescript
console.log('[Action] Користувача видалено адміністратором:', email, name);
```

## Тестування

### Чек-лист перевірки

- [ ] Доступ тільки для адміністраторів
- [ ] Глобальний пошук працює
- [ ] Всі кнопки відображаються правильно
- [ ] Забанені користувачі мають лише кнопку видалення
- [ ] Підтвердження діалогів працюють
- [ ] Toast-повідомлення відображаються
- [ ] Дані оновлюються після дій
- [ ] Неможливо видалити/забанити себе
- [ ] Неможливо змінити власну роль

## Changelog

### Version 1.0 (2025-11-12)

- ✅ Базова таблиця користувачів
- ✅ Глобальний fuzzy search
- ✅ 6 управляючих дій
- ✅ Захист для забанених користувачів
- ✅ Підтвердження через AlertDialog
- ✅ Toast-повідомлення
- ✅ Валідація прав доступу
- ✅ TypeScript типізація
- ✅ Повна документація
