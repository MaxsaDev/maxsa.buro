# Налаштування меню додатку

## 📋 Опис завдання

Розробка функціоналу редагування меню додатку через адміністративну панель. Система підтримує гнучку структуру меню, де адміністратор може створювати множину меню різних типів та ранжувати їх.

## 🎯 Типи меню в додатку

### Статичні меню (редагуються в коді)

1. **Адміністратор з секціями та пунктами** (`lib/menu.ts` → `menuAdmin.navAdminSections`)
2. **Адміністратор з пунктами** (`lib/menu.ts` → `menuAdmin.navAdminItems`)
3. **Персональне меню користувача** (`lib/menu.ts` → `menuUser.navUserProfile`)

### Динамічні меню (редагуються через БД)

Система підтримує два типи меню (визначені в `mx_dic.menu_types`):

1. **Меню з секціями та пунктами** (`sections`)
   - Структура: Меню → Категорія → Пункт меню
   - Таблиці: `mx_dic.menus` → `mx_dic.menu_user_sections_category` → `mx_dic.menu_user_sections_items`

2. **Меню з пунктами** (`items`)
   - Структура: Меню → Пункт меню
   - Таблиці: `mx_dic.menus` → `mx_dic.menu_user_items`

3. **Меню підтримки додатку** (єдине статичне меню типу `items`)
   - Таблиця: `mx_dic.menu_app_support`
   - Завжди відображається внизу сайдбара над профілем користувача

## 🗄️ Структура бази даних

### Основні таблиці

#### `mx_dic.menu_types` - Словник типів меню

```sql
- id (smallserial PRIMARY KEY)
- code (text UNIQUE) - машинний код: 'sections', 'items'
- title (text) - локалізована назва (українська)
- sort_order (int) - порядок у селектах
- is_active (boolean) - м'яка деактивація без видалення
```

#### `mx_dic.menus` - Таблиця меню

```sql
- id (SERIAL PRIMARY KEY)
- title (text) - назва меню (видно над пунктами в сайдбарі)
- menu_type_id (smallint FK) - тип меню (sections або items)
- sort_order (int) - порядок відображення меню
- is_active (boolean) - активність меню
- created_at, updated_at (timestamptz)
```

#### `mx_dic.menu_user_sections_category`

Категорії меню користувача (верхній рівень меню з секціями).

```sql
- id (SERIAL PRIMARY KEY)
- menu_id (int FK NOT NULL) - FK на mx_dic.menus.id
- title (text) - назва категорії
- url (text) - базовий URL
- icon (text) - назва іконки (frontend)
- is_active (boolean) - активність категорії
- created_at, updated_at (timestamptz)
```

#### `mx_dic.menu_user_sections_items`

Пункти меню всередині категорій.

```sql
- id (SERIAL PRIMARY KEY)
- category_id (int FK) - FK на категорію
- title (text) - назва пункту
- icon (text) - іконка пункту
- url (text) - URL пункту
- sort_order (int) - порядок сортування в межах категорії
- is_active (boolean) - активність пункту
- created_at, updated_at (timestamptz)
```

#### `mx_dic.menu_user_items`

Пункти меню користувача без категорій (простий список).

```sql
- id (SERIAL PRIMARY KEY)
- menu_id (int FK NOT NULL) - FK на mx_dic.menus.id
- title (text) - назва пункту
- icon (text) - іконка пункту
- url (text) - URL пункту
- sort_order (int) - порядок сортування
- is_active (boolean) - активність пункту
- created_at, updated_at (timestamptz)
```

#### `mx_dic.menu_app_support`

Меню підтримки та зворотнього зв'язку (єдине статичне меню).

```sql
- id (SERIAL PRIMARY KEY)
- menu_id (int FK NOT NULL) - FK на mx_dic.menus.id
- title (text) - назва пункту
- url (text) - URL пункту
- icon (text) - іконка пункту
- is_active (boolean) - активність пункту
```

### Тригери та функції

Для автоматичного управління `sort_order` створені тригери:

- **`menu_user_sections_fn.sql`** - тригери для `menu_user_sections_items`
  - Автоприсвоєння `sort_order` при INSERT
  - Перерозподіл `sort_order` при UPDATE (drag&drop)
  - Ущільнення `sort_order` після DELETE

- **`menu_user_ites_fn.sql`** - тригери для `menu_user_items`
  - Аналогічна логіка для простого списку меню

## 🏗️ Архітектура рішення

### Структура файлів

```
app/(protected)/mx-admin/(settings)/menu-app/
  └── page.tsx                    # Server Component - завантаження даних

components/mx-admin/menu/
  ├── menu-tabs-wrapper.tsx       # Client Component - обгортка для Tabs
  ├── menu-user-sections.tsx      # Компонент меню з секціями (з групуванням по меню)
  ├── menu-user-items.tsx         # Компонент меню з пунктами (з групуванням по меню)
  ├── menu-app-support.tsx        # Компонент меню підтримки (єдине меню)
  ├── create-menu-form.tsx        # Форма створення нового меню
  ├── sortable-menu-card.tsx      # Картка меню з drag&drop та управлінням
  ├── sortable-menu-wrapper.tsx   # Обгортка для drag-and-drop пунктів
  ├── add-menu-item-form.tsx      # Форма додавання пунктів меню
  └── icon-picker.tsx             # Компонент для вибору іконки з пошуком

components/ui-maxsa/
  └── edit-db-maxsa.tsx           # Універсальний компонент для inline редагування

data/mx-dic/
  └── menu-admin.ts               # Функції отримання та оновлення даних з БД

actions/mx-admin/menu/
  ├── reorder-menu-items.ts       # Server Actions для оновлення порядку
  ├── toggle-menu-active.ts       # Server Actions для переключення активності
  ├── update-menu-fields.ts       # Server Actions для оновлення полів (title, url, icon)
  ├── create-menu-items.ts        # Server Actions для створення нових пунктів меню
  └── delete-menu-items.ts        # Server Actions для видалення пунктів меню

schemas/mx-admin/
  └── menu-schema.ts              # Zod схеми валідації (title, url, icon)

lib/icon/
  └── get-menu-icon.ts            # Мапа іконок та функція отримання іконки

interfaces/mx-dic/
  ├── menu-user-sections.ts       # Типи для меню з секціями
  ├── menu-user-items.ts          # Типи для меню з пунктами
  └── menu-app-support.ts         # Типи для меню підтримки
```

### Потік даних

1. **Server Component** (`page.tsx`) завантажує дані з БД через `data/mx-dic/menu-admin.ts`
2. Дані передаються в **Client Component** (`menu-tabs-wrapper.tsx`)
3. Компонент чекає hydration перед рендерингом Tabs (уникнення помилок hydration)
4. Кожен тип меню відображається в окремій вкладці через компоненти:
   - `MenuUserSections` - меню з секціями
   - `MenuUserItems` - меню з пунктами
   - `MenuAppSupport` - меню підтримки

### Drag-and-Drop

Для зміни порядку пунктів меню використовується **@dnd-kit**:

- `SortableMenuWrapper` - обгортка з `DndContext` та `SortableContext`
- `SortableMenuItem` - окремі пункти з можливістю перетягування
- При зміні порядку викликається `reorderMenuUserSectionsItems` або `reorderMenuUserItems`
- Optimistic updates для покращення UX
- Автоматичний rollback при помилці

## 🎨 UI Компоненти

### Використані компоненти Shadcn UI

- **Tabs** - для розділення типів меню (з grid layout для однакового розміру вкладок)
- **Item** - для відображення пунктів меню
- **Switch** - для управління `is_active`
- **GripVertical** (Lucide) - іконка для drag-and-drop
- **Sheet** - для відображення вибору іконок
- **InputGroup** - для поля пошуку іконок

### Кастомні компоненти

- **EditDbMaxsa** (`components/ui-maxsa/edit-db-maxsa.tsx`) - універсальний компонент для inline редагування текстових полів
  - Підтримує стани: default, dirty (orange border), error (red border)
  - Валідація через Zod схеми
  - Автоматичне відображення кнопок збереження/скасування при змінах
  - Підтримка типів: text, url, textarea

- **IconPicker** (`components/mx-admin/menu/icon-picker.tsx`) - компонент для вибору іконки при редагуванні
  - Кнопка з поточною іконкою
  - Sheet з grid іконок (6 колонок)
  - Пошук по назві іконки
  - Відображення вибраної іконки
  - Автоматичне збереження при виборі

- **IconPickerNew** (`components/mx-admin/menu/icon-picker-new.tsx`) - компонент для вибору іконки в формах додавання
  - Аналогічний до IconPicker, але оптимізований для використання в формах
  - Використовується в AddMenuItemForm

- **AddMenuItemForm** (`components/mx-admin/menu/add-menu-item-form.tsx`) - універсальна форма додавання пунктів меню
  - Responsive дизайн: Sheet на desktop, Drawer на mobile
  - Валідація через React Hook Form + Zod
  - Підтримка додавання категорій, пунктів меню та пунктів в категоріях
  - Автоматичне очищення форми після успішного створення
  - Компактна кнопка тригера з іконкою "+"

### Структура відображення

#### Меню з секціями (`MenuUserSections`)

```
[+ Створити нове меню] - кнопка створення меню

📦 Назва меню (SortableMenuCard)
  ├── GripVertical - drag handle для меню
  ├── EditDbMaxsa (title) - редагування назви меню
  ├── Switch - управління активністю меню
  └── Trash2 - видалення меню
  └── 📁 Категорія
      ├── IconPicker (кнопка з іконкою)
      ├── EditDbMaxsa (title) - редагування назви
      ├── EditDbMaxsa (url) - редагування URL
      ├── Switch - управління активністю
      └── Trash2 - видалення категорії
      └── 📄 Пункт 1
          ├── IconPicker (кнопка з іконкою)
          ├── EditDbMaxsa (title) - редагування назви
          ├── EditDbMaxsa (url) - редагування URL
          ├── GripVertical - drag handle
          ├── Switch - управління активністю
          └── Trash2 - видалення пункту
      └── [+ Додати пункт меню] - кнопка додавання
```

#### Меню з пунктами (`MenuUserItems`)

```
[+ Створити нове меню] - кнопка створення меню

📦 Назва меню (SortableMenuCard)
  ├── GripVertical - drag handle для меню
  ├── EditDbMaxsa (title) - редагування назви меню
  ├── Switch - управління активністю меню
  └── Trash2 - видалення меню
  └── 📄 Пункт 1
      ├── IconPicker (кнопка з іконкою)
      ├── EditDbMaxsa (title) - редагування назви
      ├── EditDbMaxsa (url) - редагування URL
      ├── GripVertical - drag handle
      ├── Switch - управління активністю
      └── Trash2 - видалення пункту
  └── [+ Додати пункт меню] - кнопка додавання
```

#### Меню підтримки (`MenuAppSupport`)

```
📄 Пункт 1
  ├── IconPicker (кнопка з іконкою)
  ├── EditDbMaxsa (title) - редагування назви
  ├── EditDbMaxsa (url) - редагування URL
  ├── Switch - управління активністю
  └── Trash2 - видалення пункту
[+ Додати пункт меню] - кнопка додавання
```

**Примітка:** Меню підтримки - це єдине статичне меню, яке не може бути створено або видалено через інтерфейс. Воно завжди відображається внизу сайдбара над профілем користувача.

## 🔧 Технічні деталі

### Вирішення проблеми hydration

**Проблема:** Radix UI (Tabs) генерує випадкові ID для accessibility атрибутів, що призводить до невідповідності між серверним та клієнтським рендерингом.

**Рішення:** Створено `MenuTabsWrapper` компонент, який:

- Використовує `useState` + `useEffect` для перевірки монтування на клієнті
- До hydration показує skeleton замість Tabs
- Після hydration рендерить Tabs з даними

### Інтеграція з сайдбаром

Меню підтримки (`navAppSupport`) тепер завантажується з БД в `app/(protected)/layout.tsx`:

1. Завантаження через `getMenuAppSupport()`
2. Фільтрація тільки активних пунктів (`is_active === true`)
3. Передача в `AppSidebar` через пропси
4. Відображення внизу сайдбара через `NavSecondary`

## ✅ Реалізовано

- [x] Створення інтерфейсів для всіх типів меню
- [x] Функції отримання даних з БД
- [x] Компоненти відображення кожного типу меню
- [x] Drag-and-drop функціонал для зміни порядку
- [x] Server Actions для оновлення `sort_order`
- [x] Інтеграція меню підтримки з сайдбаром
- [x] Вирішення проблеми hydration для Tabs
- [x] Відображення всіх полів таблиць (title, url, icon, is_active)
- [x] Switch компоненти для управління активністю
- [x] **Редагування полів title та url через EditDbMaxsa**
- [x] **Редагування іконок через IconPicker з пошуком**
- [x] **Розширена мапа іконок з бібліотеки Lucide**
- [x] **Валідація полів через Zod схеми**
- [x] **Додавання нових пунктів меню через Sheet/Drawer форми**
- [x] **Видалення пунктів меню з підтвердженням через AlertDialog**
- [x] **Responsive дизайн форм (Desktop = Sheet, Mobile = Drawer)**
- [x] **Адаптивні вкладки для мобільних пристроїв**

## 📝 Примітки

- Система меню підтримує створення множини меню різних типів
- Кожне меню має власну назву, яка відображається над пунктами в сайдбарі
- Меню можна ранжувати між собою через drag-and-drop
- Меню підтримки - єдине статичне меню, яке не може бути створено або видалено через інтерфейс
- Для drag-and-drop використовується `@dnd-kit/core` та `@dnd-kit/sortable`
- Тригери БД автоматично управляють `sort_order` при змінах
- Компоненти використовують `MenuIcon` для безпечного відображення іконок
- Всі поля (title, url, icon) можна редагувати inline без окремих форм
- Іконки вибираються з розширеної бібліотеки Lucide React (понад 50 іконок)
- Валідація полів відбувається через Zod схеми перед збереженням
- При зміні назви меню автоматично оновлюється сайдбар через `revalidatePath('/', 'layout')`

## 🎯 Дії користувача

### Редагування назви пункту меню

1. Користувач клікає на поле з назвою пункту меню
2. Поле стає активним для редагування
3. При зміні значення з'являється помаранчевий border (dirty state)
4. З'являються кнопки збереження (✓) та скасування (✗)
5. При натисканні збереження:
   - Відбувається валідація через Zod схему
   - При помилці валідації з'являється червоний border та повідомлення
   - При успіху викликається Server Action для оновлення в БД
   - Показується повідомлення про результат
   - Сторінка автоматично ревалідується

### Редагування URL пункту меню

1. Аналогічно до редагування назви
2. Додаткова валідація URL (повний URL або шлях, що починається з `/`)
3. Підтримка порожнього значення для внутрішніх маршрутів

### Зміна іконки пункту меню

1. Користувач клікає на кнопку з поточною іконкою
2. Відкривається Sheet з grid іконок (6 колонок)
3. Користувач може:
   - Шукати іконку через поле пошуку (фільтрація по назві)
   - Переглядати всі доступні іконки з бібліотеки Lucide
   - Бачити вибрану іконку (виділена світло-сірим фоном)
4. При кліку на іконку:
   - Відбувається валідація назви іконки
   - Викликається Server Action для оновлення в БД
   - Показується повідомлення про результат
   - Sheet закривається автоматично
   - Сторінка автоматично ревалідується

### Зміна порядку пунктів меню

1. Користувач перетягує пункт меню за іконку GripVertical
2. Відбувається оптимістичне оновлення UI
3. Викликається Server Action для оновлення `sort_order` в БД
4. При помилці відбувається автоматичний rollback

### Управління активністю пунктів меню

1. Користувач перемикає Switch біля пункту меню
2. Викликається Server Action для оновлення `is_active` в БД
3. Показується повідомлення про результат
4. Сторінка автоматично ревалідується

### Додавання нового пункту меню

1. Користувач натискає кнопку "+ Додати пункт меню" (або "Додати категорію")
2. На desktop відкривається **Sheet** справа з формою
3. На mobile відкривається **Drawer** знизу з формою
4. Форма містить:
   - Поле вибору іконки (кнопка з поточною іконкою + label)
   - Поле назви (title)
   - Поле URL
   - Кнопки "Зберегти" та "Скасувати"
5. При заповненні форми:
   - Відбувається валідація всіх полів через Zod схеми
   - При помилці валідації показуються повідомлення про помилки
   - При успіху викликається Server Action для створення в БД
   - Форма автоматично закривається та очищається
   - Показується повідомлення про результат
   - Сторінка автоматично ревалідується
6. Для пунктів меню з секціями:
   - Кнопка додавання розташована після списку пунктів у категорії
   - При створенні автоматично встановлюється `category_id`
   - Автоматично встановлюється `sort_order` (максимальний + 100)

### Видалення пункту меню

1. Користувач натискає кнопку з іконкою кошика (Trash2) біля пункту меню
2. Відкривається **AlertDialog** з підтвердженням:
   - Заголовок: "Видалити пункт меню?" (або "Видалити категорію меню?")
   - Опис з назвою пункту, що буде видалено
   - Для категорій: попередження про каскадне видалення всіх пунктів
   - Кнопки "Скасувати" та "Видалити" (червона)
3. При підтвердженні:
   - Викликається Server Action для видалення з БД
   - Показується повідомлення про результат
   - Сторінка автоматично ревалідується
4. Для категорій:
   - При видаленні категорії автоматично видаляються всі пункти меню всередині неї (каскадне видалення)

## 📚 Бібліотека іконок

### Доступні іконки

Мапа іконок знаходиться в `lib/icon/get-menu-icon.ts` та містить понад 50 іконок з бібліотеки Lucide React, організованих за категоріями:

- **Адміністративні**: Shield, Users, ListChecks, Settings, Lock, Award, Target
- **Профільні**: BadgeCheck, CreditCard, Bell, User, Heart, Star
- **Підтримка**: LifeBuoy, Send, MessageSquare, Mail, Info
- **Навігація**: Dashboard, Profile, Home, Menu, Grid, List, ChevronRight, ChevronDown, ArrowLeft, ArrowRight
- **Контент**: File, FileText, Folder, Image, Video, Book, Bookmark, Archive, Package, Box
- **Дії**: Edit, PenTool, Plus, Trash, Search, Upload, Download, Link, CheckCircle, AlertCircle
- **Бізнес**: BarChart, TrendingUp, Briefcase, ShoppingCart, Calendar, Clock, Tag
- **Технічні**: Code, Database, Airplay, Zap, Activity, Globe
- **Інші**: MoreHorizontal, Camera, Map

### Додавання нових іконок

Для додавання нової іконки достатньо:

1. Імпортувати іконку з `lucide-react` в `lib/icon/get-menu-icon.ts`
2. Додати її в мапу `menuIconMap`

```typescript
import { NewIcon } from 'lucide-react';

export const menuIconMap = {
  // ... існуючі іконки
  NewIcon, // або з кастомною назвою: CustomName: NewIcon,
};
```

Іконка автоматично з'явиться в `IconPicker` без змін у компонентах.

## 🔒 Валідація даних

### Схеми валідації (`schemas/mx-admin/menu-schema.ts`)

#### `menuTitleSchema`

- Мінімум 1 символ, максимум 200
- Не може бути тільки пробілами
- Використовується для валідації назв пунктів та категорій меню

#### `menuUrlSchema`

- Може бути порожнім (для внутрішніх маршрутів)
- Якщо не порожнє, має бути валідним URL або шляхом (починається з `/`)
- Використовується для валідації URL пунктів меню

#### `menuIconSchema`

- Мінімум 1 символ, максимум 50
- Тільки латинські літери та цифри
- Використовується для валідації назв іконок

### Перевірка прав доступу

Всі Server Actions перевіряють:

1. Авторизацію користувача (`getCurrentUser()`)
2. Роль адміністратора (`role === 'admin'`)
3. Валідацію даних через Zod схеми
4. Успішність операції в БД

При помилці повертається `ActionStatus` з деталями помилки.

## 🔄 Оновлення даних

### Server Actions для оновлення полів

#### Title

- `updateMenuUserItemsTitleAction`
- `updateMenuUserSectionsItemsTitleAction`
- `updateMenuUserSectionsCategoryTitleAction`
- `updateMenuAppSupportTitleAction`

#### URL

- `updateMenuUserItemsUrlAction`
- `updateMenuUserSectionsItemsUrlAction`
- `updateMenuUserSectionsCategoryUrlAction`
- `updateMenuAppSupportUrlAction`

#### Icon

- `updateMenuUserItemsIconAction`
- `updateMenuUserSectionsItemsIconAction`
- `updateMenuUserSectionsCategoryIconAction`
- `updateMenuAppSupportIconAction`

### Server Actions для створення пунктів меню

#### Категорії та пункти

- `createMenuUserSectionsCategoryAction` - створення нової категорії меню з секціями
- `createMenuUserSectionsItemAction` - створення нового пункту меню в категорії
- `createMenuUserItemAction` - створення нового пункту меню користувача
- `createMenuAppSupportAction` - створення нового пункту меню підтримки

### Server Actions для видалення пунктів меню

#### Категорії та пункти

- `deleteMenuUserSectionsCategoryAction` - видалення категорії меню з секціями
- `deleteMenuUserSectionsItemAction` - видалення пункту меню в категорії
- `deleteMenuUserItemAction` - видалення пункту меню користувача
- `deleteMenuAppSupportAction` - видалення пункту меню підтримки

### Функції в data layer

Всі функції роботи з даними знаходяться в `data/mx-dic/menu-admin.ts`:

#### Оновлення

- `updateMenuUserItemsTitle/Url/Icon`
- `updateMenuUserSectionsItemsTitle/Url/Icon`
- `updateMenuUserSectionsCategoryTitle/Url/Icon`
- `updateMenuAppSupportTitle/Url/Icon`

#### Створення

- `createMenuUserSectionsCategory` - створення категорії меню з секціями
- `createMenuUserSectionsItem` - створення пункту меню в категорії (з автоматичним `sort_order`)
- `createMenuUserItem` - створення пункту меню користувача (з автоматичним `sort_order`)
- `createMenuAppSupport` - створення пункту меню підтримки

#### Видалення

- `deleteMenuUserSectionsCategory` - видалення категорії меню
- `deleteMenuUserSectionsItem` - видалення пункту меню в категорії
- `deleteMenuUserItem` - видалення пункту меню користувача
- `deleteMenuAppSupport` - видалення пункту меню підтримки

### Автоматична ревалідація

Після кожного успішного оновлення викликається:

- `revalidatePath('/mx-admin/menu-app')` - для оновлення даних на сторінці налаштувань меню
- `revalidatePath('/', 'layout')` - для оновлення сайдбару (при зміні назви меню, активності, порядку)
- `router.refresh()` - в Client Components для миттєвого оновлення UI

## 🎨 Responsive дизайн

### Форми додавання пунктів меню

Форми додавання використовують адаптивний підхід для оптимального UX на різних пристроях:

- **Desktop (≥768px)**: Використовується **Sheet** компонент, який виїжджає справа екрана
  - Ширина: `w-full sm:max-w-md`
  - Відступи: `px-6 pt-6` для header, `px-6 pb-6` для контенту
  - Зручний доступ до форми без перекриття основного контенту

- **Mobile (<768px)**: Використовується **Drawer** компонент, який виїжджає знизу екрана
  - Нативний UX для мобільних пристроїв
  - Відступи: `px-6 pt-6` для header, `px-6 pb-6` для контенту
  - Зручний доступ до форми на маленьких екранах

Визначення типу пристрою відбувається через хук `useIsMobile()` з `hooks/use-mobile.ts`.

### Адаптивні вкладки меню

Вкладки типу меню також адаптовані для різних розмірів екрана:

- **Desktop (≥768px)**:
  - `grid-cols-3` - три вкладки в один ряд
  - `h-9` - фіксована висота
  - `whitespace-nowrap` - текст без переносу

- **Mobile (<768px)**:
  - `grid-cols-1` або `flex-col` - вкладки вертикально
  - `h-auto` - автоматична висота для розтягування фону
  - `whitespace-normal` - дозволений перенос тексту
  - `text-sm` - зменшений розмір тексту

Це забезпечує коректне відображення вкладок без накладання на мобільних пристроях та повне розтягування сірого фону `TabsList` на всю ширину та висоту.

### Розміщення кнопок додавання

Кнопки додавання пунктів меню розташовані після списку пунктів для збереження симетрії з Switch компонентами:

- Компактна кнопка з іконкою "+" та текстом
- На мобільних пристроях показується тільки іконка
- На desktop показується іконка + текст
- Розмір кнопки: `size="sm"` для компактності

## 🔐 Безпека та валідація

### Перевірка прав доступу

Всі Server Actions для створення та видалення перевіряють:

1. Авторизацію користувача (`getCurrentUser()`)
2. Роль адміністратора (`role === 'admin'`)
3. Валідацію даних через Zod схеми
4. Успішність операції в БД

### Підтвердження видалення

Видалення пунктів меню вимагає явного підтвердження через AlertDialog:

- Показується назва пункту, що буде видалено
- Для категорій - попередження про каскадне видалення всіх пунктів
- Кнопка "Видалити" має деструктивний стиль (червоний колір)
- Неможливо видалити випадково без підтвердження

### Каскадне видалення

При видаленні категорії меню автоматично видаляються всі пункти меню всередині неї. Це реалізовано на рівні бази даних через каскадні обмеження або явне видалення в Server Actions.

## 🔗 Пов'язані файли

### SQL схеми

- `sql/mx_dic/menu_*.sql` - таблиці та тригери для меню

### Типи

- `interfaces/mx-dic/menu-user-sections.ts` - типи для меню з секціями
- `interfaces/mx-dic/menu-user-items.ts` - типи для меню з пунктами
- `interfaces/mx-dic/menu-app-support.ts` - типи для меню підтримки
- `interfaces/action-status.ts` - тип для результатів Server Actions

### Дані

- `data/mx-dic/menu-admin.ts` - функції отримання та оновлення даних з БД

### Дії (Server Actions)

- `actions/mx-admin/menu/menus.ts` - CRUD операції для меню (створення, редагування, видалення, ранжування)
- `actions/mx-admin/menu/create-menu-items.ts` - створення пунктів меню та категорій
- `actions/mx-admin/menu/reorder-menu-items.ts` - зміна порядку пунктів
- `actions/mx-admin/menu/toggle-menu-active.ts` - переключення активності
- `actions/mx-admin/menu/update-menu-fields.ts` - оновлення полів (title, url, icon)
- `actions/mx-admin/menu/delete-menu-items.ts` - видалення пунктів меню та категорій

### Схеми валідації

- `schemas/mx-admin/menu-schema.ts` - Zod схеми для валідації полів меню

### Компоненти UI

- `components/mx-admin/menu/menu-tabs-wrapper.tsx` - обгортка для Tabs з адаптивним дизайном
- `components/mx-admin/menu/menu-user-sections.tsx` - меню з секціями
- `components/mx-admin/menu/menu-user-items.tsx` - меню з пунктами
- `components/mx-admin/menu/menu-app-support.tsx` - меню підтримки
- `components/mx-admin/menu/sortable-menu-wrapper.tsx` - drag-and-drop обгортка
- `components/mx-admin/menu/menu-icon.tsx` - відображення іконок
- `components/mx-admin/menu/icon-picker.tsx` - вибір іконки з пошуком (для редагування)
- `components/mx-admin/menu/icon-picker-new.tsx` - вибір іконки для форм додавання
- `components/mx-admin/menu/add-menu-item-form.tsx` - універсальна форма додавання пунктів меню
- `components/ui-maxsa/edit-db-maxsa.tsx` - inline редагування полів

### Бібліотеки та утиліти

- `lib/icon/get-menu-icon.ts` - мапа іконок та функція отримання
- `lib/notifications.ts` - показ повідомлень про результати операцій

### Інтеграція

- `components/app-sidebar.tsx` - сайдбар з меню
- `app/(protected)/layout.tsx` - layout з завантаженням меню підтримки
- `app/(protected)/mx-admin/(settings)/menu-app/page.tsx` - сторінка редагування меню
